# appseccharlie.com

Charlie's static personal site.

```bash
npm ci
npm run dev
```

Open <http://127.0.0.1:4173/>.

Run the Playwright browser tests with `npm test`.

Deploy the contents of `public/` to the static host.
